// Ben Smith (bws0013)




#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <stdio.h>

/*
	Both this and my main program run correctly but becuase of version issues I could not test
	the program we are being graded against, so I do not know if this will work correctly. 
*/



std::vector<int> interprete(const std::string &src, const std::vector<int> &input);
std::vector<int> interprete(const std::string &src);

